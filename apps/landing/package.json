{
  "name": "landing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "serve -s dist -p $PORT"
  },
  "devDependencies": {
    "vite": "^8.0.5"
  },
  "dependencies": {
    "serve": "^14.2.6"
  }
}
